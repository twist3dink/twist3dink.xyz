{
  "name": "@twist3dink/design-system",
  "private": true,
  "type": "module",
  "description": "Token build pipeline for twist3dink.xyz (Style Dictionary)",
  "license": "UNLICENSED",
  "scripts": {
    "tokens:lint": "node ./build/lint-tokens.mjs",
    "tokens:validate": "node ./build/lint-tokens.mjs",
    "tokens:build": "node ./build/lint-tokens.mjs && node ./build/build-tokens.mjs",
    "tokens:clean": "node -e \"import fs from 'node:fs'; import path from 'node:path'; const p=path.resolve('outputs'); fs.rmSync(p,{recursive:true,force:true}); console.log('Removed', p);\"",
    "tokens:verify": "node ./build/verify-tokens.mjs",
    "tokens:build:experimental": "node ./build/run-build.mjs --experimental"
  },
  "dependencies": {
    "jsonschema": "^1.5.0",
    "style-dictionary": "^4.3.3"
  }
}
